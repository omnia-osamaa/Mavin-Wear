class AppConstants {
  AppConstants._(); 

  static const String appName = 'Mavin Wear';
  static const String appVersion = '1.0.0';

  static const String websiteUrl = 'https://www.mavin-wear.com';
  static const String internalDomain = 'mavin-wear.com';

  static const Duration splashDuration = Duration(milliseconds: 2500);
  static const Duration splashAnimationDuration = Duration(milliseconds: 1200);
  static const Duration pageTransitionDuration = Duration(milliseconds: 400);
  static const Duration retryDelay = Duration(milliseconds: 600);
}
